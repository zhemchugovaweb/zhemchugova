<section
  id="intro"
  class=`absolute top-0 left-0 w-full h-[100svh] flex items-center justify-center`
>
  <div class="h-full w-full relative">
    <img
      src="/logo.svg"
      alt="Alena Zhemchugova"
      class="h-[200px] w-[200px] mix-blend-difference"
    />
  </div>
</section>

<style>
  @keyframes logo {
    0% {
      opacity: 0;
    }
    50% {
      top: 50%;
      opacity: 100;
    }
    100% {
      top: 0;
      left: 50%;
      transform: translate(-50%, -50%);
      opacity: 0;
    }
  }
  @keyframes fade-in {
    100% {
      opacity: 1;
    }
  }

  @keyframes fade-out {
    0% {
      opacity: 1;
    }
    100% {
      opacity: 0;
      top: 0;
      display: none;
    }
  }
  @keyframes intro {
    from {
      opacity: 10;
    }
    to {
      opacity: 0;
      display: none;
    }
  }
  #intro {
    display: flex;
    z-index: 50;
    background-color: #142322;
    animation: fade-out 4s 1s ease-out forwards;
  }
  img {
    position: absolute;
    opacity: 100;
    opacity: 0;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation:
      fade-in 1s ease-in forwards,
      fade-out 1s 1s ease-out forwards;
  }
</style>
